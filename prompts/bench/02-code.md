# Code Generation Test

Write a Python function that takes a list of integers and returns a new list containing only the prime numbers from the original list. Include docstring and type hints. The function should be efficient and handle edge cases like empty lists and lists with no primes.
